# Dependencies & build
FE/node_modules/
FE/dist/

# Env local (chỉnh URL / Client ID trên máy bạn)
FE/.env

# Logs & OS
*.log
.DS_Store
Thumbs.db

# Script SQL nội bộ (chứa email admin/lệnh DB cục bộ — không commit)
scripts/*.sql
